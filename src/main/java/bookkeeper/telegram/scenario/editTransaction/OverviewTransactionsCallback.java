package bookkeeper.telegram.scenario.editTransaction;

class OverviewTransactionsCallback extends AbstractTransactionEditCallback {
    OverviewTransactionsCallback(long transactionId) {
        super(transactionId);
    }
}
