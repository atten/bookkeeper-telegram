package bookkeeper.telegram.scenario.editTransaction;

class ApproveTransactionCallback extends AbstractTransactionEditCallback {
    ApproveTransactionCallback(long transactionId) {
        super(transactionId);
    }
}
