package bookkeeper.telegram.scenario.editTransaction;

class SelectAccountCallback extends AbstractTransactionEditCallback {

    SelectAccountCallback(long transactionId) {
        super(transactionId);
    }
}
